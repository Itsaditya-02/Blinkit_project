# Auto detect text files and perform LF normalization
* text=auto
# Track Power BI files with Git LFS
*.pbix filter=lfs diff=lfs merge=lfs -text

# Treat text files as UTF-8 and normalize line endings
*.md text eol=lf
*.txt text eol=lf
*.csv text eol=lf

# Optional: treat JSON files as text with LF endings
*.json text eol=lf

# Optional: treat PowerShell scripts as text with LF endings
*.ps1 text eol=lf

# Treat SQL files as text
*.sql text eol=lf

# Prevent accidental execution of binaries
*.exe binary
*.dll binary

# Optional: custom linguist language for .pbix to not consider it source code
*.pbix linguist-documentation

